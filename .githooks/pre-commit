#!/bin/sh
# Ruff clean and pytest green before every commit. Enable once per clone:
#     git config core.hooksPath .githooks
set -e
uv run ruff check .
uv run ruff format --check .
uv run pytest -q -p no:cacheprovider
